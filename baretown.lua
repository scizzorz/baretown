function _init()
map(0, 0, 0, 0, 8, 8)
spr(1, 32, 32)

map(16, 16, 64, 64, 8, 8)
spr(2, 96, 96)

map(0, 16, 0, 64, 8, 8)
spr(3, 32, 96)

map(16, 0, 64, 0, 8, 8)
spr(4, 96, 32)
end

function _update60()
end
